---
# this is generated file.
title: <%= displayName; %>
<% if(description) { _%>
description: <%= description.split('\n')[0] %>
<% } _%>
layout: Docs
---

# <%= displayName; %>

> <%= isHook ? 'Hook' : 'Component' %>

## Import

```js copy
import { <%= displayName; %> } from 'morfix';
```

<%- description; %>
<%_ if(Object.keys(props).length > 0) { _%>
## <%= isHook ? 'Parameters' : 'Props' _%>

| Name | Type | Default | Description |
| ---- | ---- | ------- | ----------- |
<%_ Object.keys(props).map((name) => { _%>
| <%= name; %><%=props[name].required ? '*' : '' %> | <%- props[name].type.name.replace(/\|/g, '&#124;').replace(/</g, '&lt;').replace(/>/g, '&gt;'); %> | <%= props[name].defaultValue ? props[name].defaultValue : props[name].required ? 'Required' : '' %> | <%= props[name].description; %>
<%_ })
}_%>