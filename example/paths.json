/* 
    FIXME: this file does the trick for vscode and react-scripts
    by default, react-scripts are running nicely with local installation of morfix (file:..)
    but vscode do not detect typings for morfix, so we specify path to morfix via compilerOptions.paths.morfix
*/
{
    "$schema": "https://json.schemastore.org/tsconfig",
    "compilerOptions": {
        "baseUrl": ".",
        "paths": {
            "morfix": [".."]
        }
    }
}
